

<h1>Edit Blog</h1>

<%= form_with(model: @blog, url: blog_path(@blog), method: :patch, local: true) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :body %>
  <%= form.text_area :body %>

  <%= form.submit 'Update' %>
<% end %>
